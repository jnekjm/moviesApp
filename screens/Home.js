import React, { useEffect, useState } from "react";

import {
    ActivityIndicator,
    view,
    StyleSheet,
    Dimansions,
    Scrollview,
} from 'react-native';



const styles = StyleSheet.create({
    sliderContainer: {
       flex: 1,
       justifyContent: 'center',
       alignItems: 'center',
   
   },
   sliderStyle: {
       height: 0,
   
   },
   carousel: {
       flex: 1,
       justifyContent: 'center',
       alignItems: 'center',
   
   },
   });


   export default Home 